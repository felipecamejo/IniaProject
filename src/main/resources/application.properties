spring.application.name=Proyecto INIA

server.port=8080
server.servlet.context-path=/Inia

spring.datasource.url = jdbc:postgresql://localhost:5432/Inia
spring.datasource.username = ${DB_USER}
spring.datasource.password = ${DB_PASS}
spring.datasource.driver-class-name = org.postgresql.Driver

spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configuraci�n de la aplicaci�n
app.name=Inia

# Configuración JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# URL base del middleware Python (FastAPI)
python.middleware.base-url=http://localhost:9099