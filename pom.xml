<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.4</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>ti</groupId>
    <artifactId>proyectoInia</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>Projecto Inia</name>
    <description>Proyecto para la empresa Inia</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>21</java.version>
        <lombok.version>1.18.38</lombok.version>
        <skipFrontend>true</skipFrontend>
        <mockito.version>5.14.2</mockito.version>
        <maven.surefire.version>3.5.2</maven.surefire.version>
        <jacoco.version>0.8.12</jacoco.version>
        <testcontainers.version>1.19.8</testcontainers.version>
    </properties>
    <dependencies>
        <!-- spring-boot-starter-data-jpa already declared above; duplicated entry removed -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.7.0</version>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
            <version>2.7.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web-services</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers para tests de integración con base de datos real -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${testcontainers.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- JWT Dependencies -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt</artifactId>
            <version>0.9.1</version>
        </dependency>
        <dependency>
            <groupId>javax.xml.bind</groupId>
            <artifactId>jaxb-api</artifactId>
            <version>2.3.1</version>
        </dependency>		<!-- Jackson support for Java 8 time types -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>


        <!-- Spring Boot Mail -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.version}</version>
                <configuration>
                    <argLine>@{argLine} -XX:+EnableDynamicAgentLoading</argLine>
                    <!-- Configurar JWT_SECRET como variable de entorno para tests -->
                    <environmentVariables>
                        <JWT_SECRET>miClaveSecretaSuperSeguraParaJWT2024IniaProject</JWT_SECRET>
                    </environmentVariables>
                    <!-- Permitir ejecutar tests específicos incluso si están excluidos -->
                    <failIfNoSpecifiedTests>false</failIfNoSpecifiedTests>
                    <!-- 
                        Incluir explícitamente tests de Testcontainers usando nombres de clase claros.
                        Nota: TestcontainersConfig y AbstractTestcontainersIntegrationTest son clases de soporte
                        (no tests ejecutables) pero se compilan automáticamente desde src/test/java y están
                        disponibles en el classpath cuando se ejecutan los tests que las usan.
                    -->
                    <includes>
                        <!-- Test de conexión Testcontainers -->
                        <include>**/TestcontainersConnectionTest.java</include>
                        <!-- Cualquier test que contenga "Testcontainers" en el nombre y termine en Test -->
                        <include>**/*Testcontainers*Test.java</include>
                        <!-- Tests de integración que usan Testcontainers (extienden AbstractTestcontainersIntegrationTest) -->
                        <include>**/*IntegrationTest.java</include>
                    </includes>
                    <!-- Excluir tests CRUD básicos y tests de seguridad/autorización -->
                    <excludes>
                        <!-- Excluir tests de integración CRUD básicos (sin Testcontainers) -->
                        <exclude>**/integration/*ControllerTest.java</exclude>
                        <!-- Tests de integración con Testcontainers se INCLUYEN (no excluir) -->
                        <!-- Excluir tests de seguridad/autorización (no son casos de uso de negocio) -->
                        <exclude>**/security/**Test.java</exclude>
                        <exclude>**/security/**Service.java</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- JaCoCo Plugin para cobertura de código -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <!-- Preparar agente JaCoCo -->
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <!-- Generar reporte después de los tests -->
                    <execution>
                        <id>jacoco-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <!-- Verificar cobertura mínima -->
                    <execution>
                        <id>jacoco-check</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                        <minimum>0.05</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <excludes>
                        <!-- Excluir clases generadas por Lombok -->
                        <exclude>**/config/**</exclude>
                        <exclude>**/dto/**</exclude>
                        <exclude>**/entity/**</exclude>
                        <exclude>**/response/**</exclude>
                        <exclude>**/responses/**</exclude>
                        <!-- Excluir clases de configuración -->
                        <exclude>**/*Config*.class</exclude>
                        <exclude>**/*Configuration*.class</exclude>
                        <!-- Excluir clases de aplicación principal -->
                        <exclude>**/ProyectoIniaApplication.class</exclude>
                        <!-- Excluir clases de seguridad -->
                        <exclude>**/security/**</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!-- ★ Nuevo plugin para Angular (opcional) ★ -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>1.13.3</version>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <configuration>
                            <nodeVersion>v20.19.0</nodeVersion> <!-- Versión mínima requerida por Angular CLI (v20.19 o v22.12) -->
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                    </execution>
                    <execution>
                        <id>npm-build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <skip>${skipFrontend}</skip>
                    <workingDirectory>${project.basedir}/frontend</workingDirectory> <!-- Ruta a tu carpeta Angular -->
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- Perfiles Maven para diferentes estrategias de testing -->
    <profiles>
        <!-- Perfil por defecto: Solo casos de uso de negocio importantes -->
        <profile>
            <id>use-cases-only</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <argLine>@{argLine} -XX:+EnableDynamicAgentLoading</argLine>
                            <!-- Configurar JWT_SECRET como variable de entorno para tests -->
                            <environmentVariables>
                                <JWT_SECRET>miClaveSecretaSuperSeguraParaJWT2024IniaProject</JWT_SECRET>
                            </environmentVariables>
                            <!-- Permitir ejecutar tests específicos incluso si están excluidos -->
                            <failIfNoSpecifiedTests>false</failIfNoSpecifiedTests>
                            <!-- 
                                Incluir explícitamente tests de Testcontainers usando nombres de clase claros.
                                Nota: TestcontainersConfig y AbstractTestcontainersIntegrationTest son clases de soporte
                                (no tests ejecutables) pero se compilan automáticamente desde src/test/java y están
                                disponibles en el classpath cuando se ejecutan los tests que las usan.
                            -->
                            <includes>
                                <!-- Test de conexión Testcontainers -->
                                <include>**/TestcontainersConnectionTest.java</include>
                                <!-- Cualquier test que contenga "Testcontainers" en el nombre y termine en Test -->
                                <include>**/*Testcontainers*Test.java</include>
                                <!-- Tests de integración que usan Testcontainers (extienden AbstractTestcontainersIntegrationTest) -->
                                <include>**/*IntegrationTest.java</include>
                            </includes>
                            <!-- Excluir tests CRUD básicos y tests de seguridad/autorización -->
                            <excludes>
                                <exclude>**/integration/*ControllerTest.java</exclude>
                                <!-- Tests de integración con Testcontainers se INCLUYEN -->
                                <exclude>**/security/**Test.java</exclude>
                                <exclude>**/security/**Service.java</exclude>
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Perfil alternativo: Todos los tests (incluyendo CRUDs) -->
        <profile>
            <id>all-tests</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <!-- Asegurar que todos los perfiles reciban el JWT secreto -->
                            <environmentVariables>
                                <JWT_SECRET>miClaveSecretaSuperSeguraParaJWT2024IniaProject</JWT_SECRET>
                            </environmentVariables>
                            <!-- No excluir nada, ejecutar todos los tests -->
                            <excludes>
                                <!-- Vacío para ejecutar todos -->
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Perfil opcional: omitir tests que usan Testcontainers/integración -->
        <profile>
            <id>skip-testcontainers</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <!-- Configurar JWT_SECRET como variable de entorno para tests -->
                            <environmentVariables>
                                <JWT_SECRET>miClaveSecretaSuperSeguraParaJWT2024IniaProject</JWT_SECRET>
                            </environmentVariables>
                            <excludes>
                                <exclude>**/*Testcontainers*Test.java</exclude>
                                <exclude>**/*IntegrationTest.java</exclude>
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>